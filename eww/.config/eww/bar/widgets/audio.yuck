(defwidget audio []
  (box :class ""
       :orientation "v"
       :tooltip "Volume"
       :halign "center"
       :valign "center"
       :space-evenly "true"

      (button :class "icon" :onclick "pavucontrol &" (label :text ""))
      (button :class "texting" (label :text "${volume}%"))))
