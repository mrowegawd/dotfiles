(defwidget cpu []
  (box
       :orientation "v"
       :space-evenly false
       :tooltip "CPU"
    (button :class "icon" :onclick "bar/scripts/openwindow.sh btop &"
      (label :text "")
    )
    (button :class "texting" (label :text "${round(EWW_CPU.avg, 0)}%")
    )
  )
)
