#!/bin/bash

/usr/local/bin/dunstify --replace 31341 -i ~/.config/miscxrdb/icons/drinks.png "Jangan lupa minum"

sound_man="$HOME/.config/dunst/positive-notification-new-level-152480.mp3"

if [ -f "$sound_man" ]; then
	/usr/local/bin/mpv "$sound_man" --volume=100 2 >/dev/null
fi
